run = "npm run dev"

[[ports]]
localPort = 5000
externalPort = 80

[deployment]
run = ["sh", "-c", "node dist/index.js"]
deploymentTarget = "cloudrun"
build = ["sh", "-c", "npm install && npm run build"]
