STOP. Do not write any CSS fixes yet. First, diagnose the problem.
The entire site has a horizontal overflow issue on mobile (375px viewport). Every element — hero headline, subtitle, email input, CTA button, feature cards, trust line — is clipping past the right edge. This is NOT a component-level issue. It's a layout-level issue.
Step 1: Find the broken element. Open the preview at 375px width. Open the browser console and run:
javascriptdocument.querySelectorAll('*').forEach(el => {
  if (el.scrollWidth > document.documentElement.clientWidth) {
    console.log('OVERFLOW:', el.tagName, el.className, 'scrollWidth:', el.scrollWidth, 'clientWidth:', document.documentElement.clientWidth);
  }
});
Tell me which elements are reported before making any changes.
Step 2: Check the hero grid layout. The hero section uses a two-column layout (left: text content, right: feature cards). On mobile, this grid/flex container is NOT collapsing to a single column. Find the CSS rule that controls the hero's column layout and check:

Is there a grid-template-columns or display: flex with a fixed width that doesn't have a mobile breakpoint?
Is there a min-width on any child that prevents shrinking?
Is there a width or max-width set in pixels rather than percentages?

Step 3: Apply the fix. The hero section's two-column layout needs a mobile breakpoint. The fix will look something like:
css@media (max-width: 768px) {
  /* Replace .hero-grid with the actual class name from Step 1 */
  .hero-grid {
    grid-template-columns: 1fr;  /* or flex-direction: column */
    max-width: 100vw;
    overflow-x: hidden;
    padding: 0 20px;
    box-sizing: border-box;
  }
}
Step 4: Verify. After the fix, run the console script from Step 1 again. Zero elements should report overflow. Check at 375px (iPhone SE), 390px (iPhone 14), and 428px (iPhone 14 Pro Max).
Do NOT skip to Step 3. I need you to find the actual class names and the actual CSS property causing the overflow. Previous fixes failed because they targeted the wrong selectors.